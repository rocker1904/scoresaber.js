<!-- Describe the change and why it's needed. -->

## Checklist

- [ ] Tests pass (`npm test`) and types/lint are clean (`npm run typecheck && npm run lint`)
- [ ] Public API change? Updated the report (`npm run api:update`) and added a changeset (`npx changeset`)
- [ ] Touched the API spec? Regenerated types (`npm run gen:types`)
